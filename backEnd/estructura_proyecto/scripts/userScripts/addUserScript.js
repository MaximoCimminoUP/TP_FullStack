/*const { addUser } = require('../../controllers/user'); 

// Extracts command-line arguments
const [, , email, name, lastname, isActive, roles, password] = process.argv;

// Calls the addUser function with provided arguments
addUser(email, name, lastname, isActive, roles.split(','), password);*/
